=== SiteOrigin Site Packs Addon ===

Handles exporting Site Packs.

Activate the plugin and then view the front end of the website. Append the following to the url: `?action=download`, for example `http://so.local/?action=download` and then hit enter. This plugin will throw an error if the WordPress installation in question doesn't have multisite enabled.